/**
 * El hueco del overlay cuando no hay ninguna ficha abierta.
 *
 * Next pide un `default` para cada ranura paralela: sin esto, recargar el
 * catálogo con una ficha abierta deja la ranura sin nada que pintar y la
 * página entera da error.
 */
export default function SinModal() {
  return null;
}
